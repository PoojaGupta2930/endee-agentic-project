# endee-agentic-project
This project implements a Retrieval-Augmented Generation (RAG) AI system.  It uses Streamlit for the UI, FastAPI as the backend API, Ollama for LLM generation,  and vector embeddings for semantic search.
